services:
  postgres:
    image: postgres:16-alpine
    container_name: exchange_stream_pg
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: exchange_stream
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d exchange_stream"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
